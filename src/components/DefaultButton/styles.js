import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
      width: '100%',
      marginBottom: 8,
      backgroundColor: '#FED43D',
      padding: 24,
      alignItems: 'center',
      borderRadius: 12
    }
})