import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
      width: '100%',
      marginBottom: 8,
      backgroundColor: '#FED43D',
      padding: 20,
      alignItems: 'center',
      borderRadius: 12,
      shadowColor: '#000000',
      shadowOpacity: 0.25,
      shadowRadius: 3.84,
      elevation: 5,
      shadowOffset: {
        width: 0,
        height: 2,
      }
    }
})
