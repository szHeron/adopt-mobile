import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
      width: '100%',
      marginBottom: 8,
      backgroundColor: '#fff',
      padding: 16,
      borderColor: '#A5A5A5', 
      borderWidth: 1,
      borderRadius: 12,
      color: '#A5A5A5'
    }
})